[project]
name = "slclient"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.45.1",
]

[dependency-groups]
dev = [
    "watchdog>=6.0.0",
]
