[project]
name = "catalog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Joseph.Cate", email = "joseph.cate@usda.gov" }
]
requires-python = ">=3.12"
dependencies = [
    "arrow>=1.3.0",
    "bs4>=0.0.2",
    "python-dotenv[cli]>=1.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
