[project]
name = "catalog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"

authors = [
    { name = "Joseph.Cate", email = "acate@arizona.edu" }
]

requires-python = ">=3.12"

dependencies = [
    "arrow>=1.3.0",
    "bs4>=0.0.2",
    "lxml>=5.3.1",
    "python-dotenv[cli]>=1.0.1",
    "requests>=2.32.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff>=0.11.2",
]
